######################################################################
# Input Boolean used to switch between Toggle Page 1 and Toggle Page 2
# Pressing the Togglebulb in the NavBar will switch between Toggles Page 1 and 2
input_boolean:
  hasp_plate01_p6_switchtoggles:
    initial: off

##############################################################################
# Automations for handling toggle state buttons on page 6
automation:
  # Toggles input_boolean which switches between Toggle Page 1 and Toggle Page 2
  - alias: hasp_plate01_p6_SwitchTogglesPages
    trigger:
    - platform: mqtt
      topic: 'hasp/plate01/state/p[6].b[2]'
      payload: 'ON'
    action:
    - service: input_boolean.toggle
      entity_id: input_boolean.hasp_plate01_p6_switchtoggles
  #Set font and text for toggle buttons on device connection
  - alias: hasp_plate01_p6_ToggleInit
    trigger:
    - platform: state
      entity_id: 'binary_sensor.plate01_connected'
      to: 'on'
    - platform: homeassistant
      event: start
    - platform: state
      entity_id: 'input_boolean.hasp_plate01_p6_switchtoggles'
    action:
    # Label Toggle 1/7
    - service: mqtt.publish
      data:
        topic: 'hasp/plate01/command/p[6].b[10].font'
        payload: '7'
    - service: mqtt.publish
      data:
        topic: 'hasp/plate01/command/p[6].b[10].txt'
        payload_template: >-
          {% if states.input_boolean.hasp_plate01_p6_switchtoggles.state == "off" -%}
            "{{ state_attr('group.hasp_plate01_p6_toggle1', 'friendly_name') }}"
          {% else %}
            "{{ state_attr('group.hasp_plate01_p6_toggle7', 'friendly_name') }}"
          {% endif %}
    #Label Toggle 2/8
    - service: mqtt.publish
      data:
        topic: 'hasp/plate01/command/p[6].b[11].font'
        payload: '7'
    - service: mqtt.publish
      data:
        topic: 'hasp/plate01/command/p[6].b[11].txt'
        payload_template: >-
          {% if states.input_boolean.hasp_plate01_p6_switchtoggles.state == "off" -%}
            "{{ state_attr('group.hasp_plate01_p6_toggle2', 'friendly_name') }}"
          {%- else -%}
            "{{ state_attr('group.hasp_plate01_p6_toggle8', 'friendly_name') }}"
          {%- endif %}
    #Label Toggle 3/9
    - service: mqtt.publish
      data:
        topic: 'hasp/plate01/command/p[6].b[12].font'
        payload: '7'
    - service: mqtt.publish
      data:
        topic: 'hasp/plate01/command/p[6].b[12].txt'
        payload_template: >-
          {% if states.input_boolean.hasp_plate01_p6_switchtoggles.state == "off" -%}
            "{{ state_attr('group.hasp_plate01_p6_toggle3', 'friendly_name') }}"
          {%- else -%}
            "{{ state_attr('group.hasp_plate01_p6_toggle9', 'friendly_name') }}"
          {%- endif %}
    #Label Toggle 4/10
    - service: mqtt.publish
      data:
        topic: 'hasp/plate01/command/p[6].b[13].font'
        payload: '7'
    - service: mqtt.publish
      data:
        topic: 'hasp/plate01/command/p[6].b[13].txt'
        payload_template: >-
          {% if states.input_boolean.hasp_plate01_p6_switchtoggles.state == "off" -%}
            "{{ state_attr('group.hasp_plate01_p6_toggle4', 'friendly_name') }}"
          {%- else -%}
            "{{ state_attr('group.hasp_plate01_p6_toggle10', 'friendly_name') }}"
          {%- endif %}
    #Label Toggle 5/11
    - service: mqtt.publish
      data:
        topic: 'hasp/plate01/command/p[6].b[14].font'
        payload: '7'
    - service: mqtt.publish
      data:
        topic: 'hasp/plate01/command/p[6].b[14].txt'
        payload_template: >-
          {% if states.input_boolean.hasp_plate01_p6_switchtoggles.state == "off" -%}
            "{{ state_attr('group.hasp_plate01_p6_toggle5', 'friendly_name') }}"
          {%- else -%}
            "{{ state_attr('group.hasp_plate01_p6_toggle11', 'friendly_name') }}"
          {%- endif %}
    #Label Toggle 6/12
    - service: mqtt.publish
      data:
        topic: 'hasp/plate01/command/p[6].b[15].font'
        payload: '7'
    - service: mqtt.publish
      data:
        topic: 'hasp/plate01/command/p[6].b[15].txt'
        payload_template: >-
          {% if states.input_boolean.hasp_plate01_p6_switchtoggles.state == "off" -%}
            "{{ state_attr('group.hasp_plate01_p6_toggle6', 'friendly_name') }}"
          {%- else -%}
            "{{ state_attr('group.hasp_plate01_p6_toggle12', 'friendly_name') }}"
          {%- endif %}


##############################################################################################
# Actions for Toggle Pressed

  # Toggle Toggle1/7 when p[6].b[4] pressed
  - alias: hasp_plate01_p6_Toggle1_7
    trigger:
    - platform: mqtt
      topic: 'hasp/plate01/state/p[6].b[4]'
      payload: 'ON'
    action:
    - service: homeassistant.toggle
      data_template:
        entity_id: >-
          {% if states.input_boolean.hasp_plate01_p6_switchtoggles.state == 'off' -%}
            group.hasp_plate01_p6_toggle1
          {%- else -%}
            group.hasp_plate01_p6_toggle7
          {%- endif %}

  # Toggle Toggle2 when p[6].b[5] pressed
  - alias: hasp_plate01_p6_Toggle2
    trigger:
    - platform: mqtt
      topic: 'hasp/plate01/state/p[6].b[5]'
      payload: 'ON'
    action:
    - service: homeassistant.toggle
      data_template: 
        entity_id: >-
          {% if states.input_boolean.hasp_plate01_p6_switchtoggles.state == 'off' -%}
            group.hasp_plate01_p6_toggle2
          {%- else -%}
            group.hasp_plate01_p6_toggle8
          {%- endif %}

  # Toggle Toggle3 when p[6].b[6] pressed
  - alias: hasp_plate01_p6_Toggle3
    trigger:
    - platform: mqtt
      topic: 'hasp/plate01/state/p[6].b[6]'
      payload: 'ON'
    action:
    - service: homeassistant.toggle
      data_template:
        entity_id:  >-
          {% if states.input_boolean.hasp_plate01_p6_switchtoggles.state == 'off' -%}
            group.hasp_plate01_p6_toggle3
          {%- else -%}
            group.hasp_plate01_p6_toggle9
          {%- endif %}
      
   # Toggle Button 4 when p6.b7 pressed
  - alias: hasp_plate01_p6_Toggle4
    trigger:
    - platform: mqtt
      topic: 'hasp/plate01/state/p[6].b[7]'
      payload: 'ON'
    action:
    - service: homeassistant.toggle
      data_template:
        entity_id: >-
          {% if states.input_boolean.hasp_plate01_p6_switchtoggles.state == "off" -%}
            group.hasp_plate01_p6_toggle4
          {%- else -%}
            group.hasp_plate01_p6_toggle10
          {%- endif %} 
      
   # Toggle Button 5 when p6.b8
  - alias: hasp_plate01_p6_Toggle5_13
    trigger:
    - platform: mqtt
      topic: 'hasp/plate01/state/p[6].b[8]'
      payload: 'ON'
    action:
    - service: homeassistant.toggle
      data_template:
        entity_id: >-
          {% if states.input_boolean.hasp_plate01_p6_switchtoggles.state == "off" -%}
            group.hasp_plate01_p6_toggle5
          {%- else -%}
            group.hasp_plate01_p6_toggle11
          {%- endif %} 
    
    # Toggle Button 6 p4.b9
  - alias: hasp_plate01_p6_Toggle6_14
    trigger:
    - platform: mqtt
      topic: 'hasp/plate01/state/p[6].b[9]'
      payload: 'ON'
    action:
    - service: homeassistant.toggle
      data_template:
        entity_id: >-
          {% if states.input_boolean.hasp_plate01_p6_switchtoggles.state == "off" -%}
            group.hasp_plate01_p6_toggle6
          {%- else -%}
            group.hasp_plate01_p6_toggle12
          {%- endif %} 
 
 
#########################################################################################################
#####################################################
# Toggle Color and State Picture



  # Toggle colors on p[6].b[4] when Toggle [1] changes
  - alias: hasp_plate01_p6_ToggleColor1
    trigger:
    - platform: state
      entity_id: group.hasp_plate01_p6_toggle1
    - platform: state
      entity_id: 'binary_sensor.plate01_connected'
      to: 'on'
    - platform: state
      entity_id: input_boolean.hasp_plate01_p6_switchtoggles
      to: 'off'
    - platform: homeassistant
      event: start
    condition:
      condition: state
      entity_id: input_boolean.hasp_plate01_p6_switchtoggles
      state: 'off'
    action:
    - wait_template: {{ is_state('input_boolean.hasp_plate01_p6_switchtoggles', 'off')}}
    - service: mqtt.publish
      data:
        topic: 'hasp/plate01/command/p[6].b[4].picc'
        payload_template: >-
         {% if is_state('group.hasp_plate01_p6_toggle1', 'on') and ('input_boolean.hasp_plate01_p6_switchtoggles', 'off') -%}
            {{ 1 }}
          {% elif is_state('group.hasp_plate01_p6_toggle1', 'off') and ('input_boolean.hasp_plate01_p6_switchtoggles', 'off') -%}
            {{ 2 }}
          {%- endif %}

  # Toggle colors on p[6].b[5] when Toggle [2] changes
  - alias: hasp_plate01_p6_ToggleColor2
    trigger:
    - platform: state
      entity_id: group.hasp_plate01_p6_toggle2
    - platform: state
      entity_id: 'binary_sensor.plate01_connected'
      to: 'on'
    - platform: state
      entity_id: input_boolean.hasp_plate01_p6_switchtoggles
      to: 'off'
    - platform: homeassistant
      event: start
    condition:
      condition: state
      entity_id: input_boolean.hasp_plate01_p6_switchtoggles
      state: 'off'
    action:
    - wait_template: {{ is_state('input_boolean.hasp_plate01_p6_switchtoggles', 'off')}}
    - service: mqtt.publish
      data:
        topic: 'hasp/plate01/command/p[6].b[5].picc'
        payload_template: >-
          {% if is_state('group.hasp_plate01_p6_toggle2', 'on') and ('input_boolean.hasp_plate01_p6_switchtoggles', 'off') -%}
            {{ 1 }}
          {% elif is_state('group.hasp_plate01_p6_toggle2', 'off') and ('input_boolean.hasp_plate01_p6_switchtoggles', 'off') -%}
            {{ 2 }}
          {%- endif %}

  # Toggle colors on p[6].b[6] when Toggle [3] changes
  - alias: hasp_plate01_p6_ToggleColor3
    trigger:
    - platform: state
      entity_id: group.hasp_plate01_p6_toggle3
    - platform: state
      entity_id: input_boolean.hasp_plate01_p6_switchtoggles
      to: 'off'
    - platform: state
      entity_id: 'binary_sensor.plate01_connected'
      to: 'on'
    - platform: homeassistant
      event: start
    condition:
      condition: state
      entity_id: input_boolean.hasp_plate01_p6_switchtoggles
      state: 'off'
    action:
    - wait_template: {{ is_state('input_boolean.hasp_plate01_p6_switchtoggles', 'off')}}
    - service: mqtt.publish
      data:
        topic: 'hasp/plate01/command/p[6].b[6].picc'
        payload_template: >-
          {% if is_state('group.hasp_plate01_p6_toggle3', 'on') and ('input_boolean.hasp_plate01_p6_switchtoggles', 'off') -%}
            {{ 1 }}
          {%- else -%}
            {{ 2 }}
          {%- endif %}
          
  # Toggle colors on p[6].b[7] when Toggle [4] changes
  - alias: hasp_plate01_p6_ToggleColor4
    trigger:
    - platform: state
      entity_id: group.hasp_plate01_p6_toggle4
    - platform: state
      entity_id: input_boolean.hasp_plate01_p6_switchtoggles
      to: 'off'
    - platform: state
      entity_id: 'binary_sensor.plate01_connected'
      to: 'on'
    - platform: homeassistant
      event: start
    condition:
      condition: state
      entity_id: input_boolean.hasp_plate01_p6_switchtoggles
      state: 'off'
    action:
    - wait_template: {{ is_state('input_boolean.hasp_plate01_p6_switchtoggles', 'off')}}
    - service: mqtt.publish
      data:
        topic: 'hasp/plate01/command/p[6].b[7].picc'
        payload_template: >-
          {% if is_state('group.hasp_plate01_p6_toggle4', 'on') and ('input_boolean.hasp_plate01_p6_switchtoggles', 'off') -%}
            {{ 1 }}
          {% elif is_state('group.hasp_plate01_p6_toggle4', 'off') and ('input_boolean.hasp_plate01_p6_switchtoggles', 'off') -%}
            {{ 2 }}
          {%- endif %}
   
  # Toggle colors on p[6].b[8] when Toggle [5] changes
  - alias: hasp_plate01_p6_ToggleColor5
    trigger:
    - platform: state
      entity_id: group.hasp_plate01_p6_toggle5
    - platform: state
      entity_id: input_boolean.hasp_plate01_p6_switchtoggles
      to: 'off'
    - platform: state
      entity_id: 'binary_sensor.plate01_connected'
      to: 'on'
    - platform: homeassistant
      event: start
    condition:
      condition: state
      entity_id: input_boolean.hasp_plate01_p6_switchtoggles
      state: 'off'
    action:
    - wait_template: {{ is_state('input_boolean.hasp_plate01_p6_switchtoggles', 'off')}}
    - service: mqtt.publish
      data:
        topic: 'hasp/plate01/command/p[6].b[8].picc'
        payload_template: >-
          {% if is_state('group.hasp_plate01_p6_toggle5', 'on') and ('input_boolean.hasp_plate01_p6_switchtoggles', 'off') -%}
            {{ 1 }}
          {% elif is_state('group.hasp_plate01_p6_toggle5', 'off') and ('input_boolean.hasp_plate01_p6_switchtoggles', 'off') -%}
            {{ 2 }}
          {%- endif %}
          
  # Toggle colors on p[6].b[9] when Toggle [6] changes
  - alias: hasp_plate01_p6_ToggleColor6
    trigger:
    - platform: state
      entity_id: group.hasp_plate01_p6_toggle6
    - platform: state
      entity_id: input_boolean.hasp_plate01_p6_switchtoggles
      to: 'off'
    - platform: state
      entity_id: 'binary_sensor.plate01_connected'
      to: 'on'
    - platform: homeassistant
      event: start
    condition:
      condition: state
      entity_id: input_boolean.hasp_plate01_p6_switchtoggles
      state: 'off'
    action:
    - wait_template: {{ is_state('input_boolean.hasp_plate01_p6_switchtoggles', 'off')}}
    - service: mqtt.publish
      data:
        topic: 'hasp/plate01/command/p[6].b[9].picc'
        payload_template: >-
          {% if is_state('group.hasp_plate01_p6_toggle6', 'on') and ('input_boolean.hasp_plate01_p6_switchtoggles', 'off') -%}
            {{ 1 }}
          {% elif is_state('group.hasp_plate01_p6_toggle6', 'off') and ('input_boolean.hasp_plate01_p6_switchtoggles', 'off') -%}
            {{ 2 }}
          {%- endif %}

 ########################################################## 
 
  # Toggle colors on p[6].b[10] when Toggle [7] changes
  - alias: hasp_plate01_p6_ToggleColor7
    trigger:
    - platform: state
      entity_id: group.hasp_plate01_p6_toggle7
    - platform: state
      entity_id: input_boolean.hasp_plate01_p6_switchtoggles
      to: 'off'
    - platform: state
      entity_id: 'binary_sensor.plate01_connected'
      to: 'on'
    - platform: homeassistant
      event: start
    condition:
      condition: state
      entity_id: input_boolean.hasp_plate01_p6_switchtoggles
      state: 'on'
    action:
    - wait_template: {{ is_state('input_boolean.hasp_plate01_p6_switchtoggles', 'on')}}
    - service: mqtt.publish
      data:
        topic: 'hasp/plate01/command/p[6].b[4].picc'
        payload_template: >-
          {% if is_state('group.hasp_plate01_p6_toggle7', 'on') and ('input_boolean.hasp_plate01_p6_switchtoggles', 'on') -%}
            {{ 1 }}
          {% elif is_state('group.hasp_plate01_p6_toggle7', 'off') and ('input_boolean.hasp_plate01_p6_switchtoggles', 'on') -%}
            {{ 2 }}
          {%- endif %}
         
  # Toggle colors on p[6].b[11] when Toggle [8] changes
  - alias: hasp_plate01_p6_ToggleColor8
    trigger:
    - platform: state
      entity_id: group.hasp_plate01_p6_toggle8
    - platform: state
      entity_id: input_boolean.hasp_plate01_p6_switchtoggles
      to: 'off'
    - platform: state
      entity_id: 'binary_sensor.plate01_connected'
      to: 'on'
    - platform: homeassistant
      event: start
    condition:
      condition: state
      entity_id: input_boolean.hasp_plate01_p6_switchtoggles
      state: 'on'
    action:
    - wait_template: {{ is_state('input_boolean.hasp_plate01_p6_switchtoggles', 'on')}}
    - service: mqtt.publish
      data:
        topic: 'hasp/plate01/command/p[6].b[5].picc'
        payload_template: >-
          {% if is_state('group.hasp_plate01_p6_toggle8', 'on') and ('input_boolean.hasp_plate01_p6_switchtoggles', 'on') -%}
            {{ 1 }}
          {% elif is_state('group.hasp_plate01_p6_toggle8', 'off') and ('input_boolean.hasp_plate01_p6_switchtoggles', 'on') -%}
            {{ 2 }}
          {%- endif %}


# Toggle colors on p[6].b[4] when Toggle [9] changes
  - alias: hasp_plate01_p6_ToggleColor9
    trigger:
    - platform: state
      entity_id: input_boolean.hasp_plate01_p6_switchtoggles
      to: 'on'
    - platform: state
      entity_id: group.hasp_plate01_p6_toggle9
    - platform: state
      entity_id: 'binary_sensor.plate01_connected'
      to: 'on'
    - platform: homeassistant
      event: start
    condition:
      condition: state
      entity_id: input_boolean.hasp_plate01_p6_switchtoggles
      state: 'on'
    action:
    - wait_template: {{ is_state('input_boolean.hasp_plate01_p6_switchtoggles', 'on')}}
    - service: mqtt.publish
      data:
        topic: 'hasp/plate01/command/p[6].b[6].picc'
        payload_template: >-
          {% if is_state('group.hasp_plate01_p6_toggle9', 'on') and ('input_boolean.hasp_plate01_p6_switchtoggles', 'on') -%}
            {{ 1 }}
          {% elif is_state('group.hasp_plate01_p6_toggle9', 'off') and ('input_boolean.hasp_plate01_p6_switchtoggles', 'on') -%}
            {{ 2 }}
          {%- endif %}

# Toggle colors on p[6].b[5] when Toggle [10] changes
  - alias: hasp_plate01_p6_ToggleColor10
    trigger:
    - platform: state
      entity_id: input_boolean.hasp_plate01_p6_switchtoggles
      to: 'on'
    - platform: state
      entity_id: group.hasp_plate01_p6_toggle10
    - platform: state
      entity_id: 'binary_sensor.plate01_connected'
      to: 'on'
    - platform: homeassistant
      event: start
    condition:
      condition: state
      entity_id: input_boolean.hasp_plate01_p6_switchtoggles
      state: 'on'
    action:
    - wait_template: {{ is_state('input_boolean.hasp_plate01_p6_switchtoggles', 'on')}}
    - service: mqtt.publish
      data:
        topic: 'hasp/plate01/command/p[6].b[7].picc'
        payload_template: >-
          {% if is_state('group.hasp_plate01_p6_toggle10', 'on') and ('input_boolean.hasp_plate01_p6_switchtoggles', 'on') -%}
            {{ 1 }}
          {% elif is_state('group.hasp_plate01_p6_toggle10', 'off') and ('input_boolean.hasp_plate01_p6_switchtoggles', 'on') -%}
            {{ 2 }}
          {%- endif %}

# Toggle colors on p[6].b[6] when Toggle [11] changes
  - alias: hasp_plate01_p6_ToggleColor11
    trigger:
    - platform: state
      entity_id: input_boolean.hasp_plate01_p6_switchtoggles
      to: 'on'
    - platform: state
      entity_id: group.hasp_plate01_p6_toggle11
    - platform: state
      entity_id: 'binary_sensor.plate01_connected'
      to: 'on'
    - platform: homeassistant
      event: start
    condition:
      condition: state
      entity_id: input_boolean.hasp_plate01_p6_switchtoggles
      state: 'on'
    action:
    - wait_template: {{ is_state('input_boolean.hasp_plate01_p6_switchtoggles', 'on')}}
    - service: mqtt.publish
      data:
        topic: 'hasp/plate01/command/p[6].b[8].picc'
        payload_template: >-
          {% if is_state('group.hasp_plate01_p6_toggle11', 'on') and ('input_boolean.hasp_plate01_p6_switchtoggles', 'on') -%}
            {{ 1 }}
          {% elif is_state('group.hasp_plate01_p6_toggle11', 'off') and ('input_boolean.hasp_plate01_p6_switchtoggles', 'on') -%}
            {{ 2 }}
          {%- endif %}
          
# Toggle colors on p[6].b[6] when Toggle [12] changes
  - alias: hasp_plate01_p6_ToggleColor12
    trigger:
    - platform: state
      entity_id: input_boolean.hasp_plate01_p6_switchtoggles
      to: 'on'
    - platform: state
      entity_id: group.hasp_plate01_p6_toggle12
    - platform: state
      entity_id: 'binary_sensor.plate01_connected'
      to: 'on'
    - platform: homeassistant
      event: start
    condition:
      condition: state
      entity_id: input_boolean.hasp_plate01_p6_switchtoggles
      state: 'on'
    action:
    - wait_template: {{ is_state('input_boolean.hasp_plate01_p6_switchtoggles', 'on')}}
    - service: mqtt.publish
      data:
        topic: 'hasp/plate01/command/p[6].b[9].picc'
        payload_template: >-
          {% if is_state('group.hasp_plate01_p6_toggle12', 'on') and ('input_boolean.hasp_plate01_p6_switchtoggles', 'on') -%}
            {{ 1 }}
          {% elif is_state('group.hasp_plate01_p6_toggle12', 'off') and ('input_boolean.hasp_plate01_p6_switchtoggles', 'on') -%}}
            {{ 2 }}
          {%- endif %}

