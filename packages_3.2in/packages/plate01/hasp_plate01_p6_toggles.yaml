######################################################################
# Input Boolean used to switch between Toggle Page 1 and Toggle Page 2
# Pressing the Togglebulb in the NavBar will switch between Toggles Page 1 and 2
input_boolean:
  hasp_plate01_p6_switchtoggles:
    initial: off

##############################################################################
# Automations for handling toggle state buttons on page 6
automation:
  # Toggles input_boolean which switches between Toggle Page 1 and Toggle Page 2
  #- alias: hasp_plate01_p6_SwitchTogglesPages
  #  trigger: 
  #  - platform: mqtt
  #    topic: 'hasp/plate01/state/p[6].b[2]'
  #    payload: 'ON'
  #  action:
  #  - service: input_boolean.toggle
  #    entity_id: input_boolean.hasp_plate01_p6_switchtoggles
  #Set font and text for toggle buttons on device connection
  - alias: hasp_plate01_p6_ToggleInit
    trigger:
    - platform: state
      entity_id: 'binary_sensor.plate01_connected'
      to: 'on'
    - platform: homeassistant
      event: start
    - platform: state
      entity_id: 'input_boolean.hasp_plate01_p6_switchtoggles'
    action:
    # Label Toggle 1/9
    - service: mqtt.publish
      data:
        topic: 'hasp/plate01/command/p[6].b[12].font'
        payload: '7'
    - service: mqtt.publish
      data:
        topic: 'hasp/plate01/command/p[6].b[12].txt'
        payload_template: >-
          {% if states.input_boolean.hasp_plate01_p6_switchtoggles.state == "off" -%}
            "TOGGLENAME1"
          {% else %}
            "TOGGLENAME9"
          {% endif %}
    #Label Toggle 2/10
    - service: mqtt.publish
      data:
        topic: 'hasp/plate01/command/p[6].b[13].font'
        payload: '7'
    - service: mqtt.publish
      data:
        topic: 'hasp/plate01/command/p[6].b[13].txt'
        payload_template: >-
          {% if states.input_boolean.hasp_plate01_p6_switchtoggles.state == "off" -%}
            "TOGGLENAME2"
          {%- else -%}
            "TOGGLENAME10"
          {%- endif %}
    #Label Toggle 3/11
    - service: mqtt.publish
      data:
        topic: 'hasp/plate01/command/p[6].b[14].font'
        payload: '7'
    - service: mqtt.publish
      data:
        topic: 'hasp/plate01/command/p[6].b[14].txt'
        payload_template: >-
          {% if states.input_boolean.hasp_plate01_p6_switchtoggles.state == "off" -%}
            "TOGGLENAME3"
          {%- else -%}
            "TOGGLENAME11"
          {%- endif %}
    #Label Toggle 4/12
    - service: mqtt.publish
      data:
        topic: 'hasp/plate01/command/p[6].b[15].font'
        payload: '7'
    - service: mqtt.publish
      data:
        topic: 'hasp/plate01/command/p[6].b[15].txt'
        payload_template: >-
          {% if states.input_boolean.hasp_plate01_p6_switchtoggles.state == "off" -%}
            "TOGGLENAME4"
          {%- else -%}
            "TOGGLENAME12"
          {%- endif %}
    #Label Toggle 5/13
    - service: mqtt.publish
      data:
        topic: 'hasp/plate01/command/p[6].b[16].font'
        payload: '7'
    - service: mqtt.publish
      data:
        topic: 'hasp/plate01/command/p[6].b[16].txt'
        payload_template: >-
          {% if states.input_boolean.hasp_plate01_p6_switchtoggles.state == "off" -%}
            "TOGGLENAME5"
          {%- else -%}
            "TOGGLENAME13"
          {%- endif %}
    #Label Toggle 6/14
    - service: mqtt.publish
      data:
        topic: 'hasp/plate01/command/p[6].b[17].font'
        payload: '7'
    - service: mqtt.publish
      data:
        topic: 'hasp/plate01/command/p[6].b[17].txt'
        payload_template: >-
          {% if states.input_boolean.hasp_plate01_p6_switchtoggles.state == "off" -%}
            "TOGGLENAME6"
          {%- else -%}
            "TOGGLENAME14"
          {%- endif %}
    #Label Toggle 7/15
    - service: mqtt.publish
      data:
        topic: 'hasp/plate01/command/p[6].b[18].font'
        payload: '7'
    - service: mqtt.publish
      data:
        topic: 'hasp/plate01/command/p[6].b[18].txt'
        payload_template: >-
          {% if states.input_boolean.hasp_plate01_p6_switchtoggles.state == "off" -%}
            "TOGGLENAME7"
          {%- else -%}
            "TOGGLENAME15"
          {%- endif %}
    #Label Toggle 8/16
    - service: mqtt.publish
      data:
        topic: 'hasp/plate01/command/p[6].b[19].font'
        payload: '7'
    - service: mqtt.publish
      data:
        topic: 'hasp/plate01/command/p[6].b[19].txt'
        payload_template: >-
          {% if states.input_boolean.hasp_plate01_p6_switchtoggles.state == "off" -%}
            "TOGGLENAME8"
          {%- else -%}
            "TOGGLENAME16"
          {%- endif %}


##############################################################################################
# Actions for Toggle on if pressed

  # Toggle Toggle1/9 when p[6].b[4] pressed
  - alias: hasp_plate01_p6_Toggle1
    trigger:
    - platform: mqtt
      topic: 'hasp/plate01/state/p[6].b[4]'
      payload: 'ON'
    action:
    - service: homeassistant.turn_on
      data_template:
        entity_id: >-
          {% if is_state('domain.TOGGLE1', 'off') and ('input_boolean.hasp_plate01_p6_switchtoggles', 'off') -%}
            domain.TOGGLE1
          {%- elif is_state('domain.TOGGLE9', 'off') and ('input_boolean.hasp_plate01_p6_switchtoggles', 'on') -%}
            domain.TOGGLE9
          {%- endif %}

  # Toggle Toggle2 when p[6].b[5] pressed
  - alias: hasp_plate01_p6_Toggle2
    trigger:
    - platform: mqtt
      topic: 'hasp/plate01/state/p[6].b[5]'
      payload: 'ON'
    action:
    - service: homeassistant.turn_on
      data_template: 
        entity_id: >-
          {% if is_state('domain.TOGGLE2', 'off') and ('input_boolean.hasp_plate01_p6_switchtoggles', 'off') -%}
            domain.TOGGLE2
          {%- elif is_state('domain.TOGGLE10', 'off') and ('input_boolean.hasp_plate01_p6_switchtoggles', 'on') -%}
            domain.TOGGLE10
          {%- endif %}

  # Toggle Toggle3 when p[6].b[6] pressed
  - alias: hasp_plate01_p6_Toggle3
    trigger:
    - platform: mqtt
      topic: 'hasp/plate01/state/p[6].b[6]'
      payload: 'ON'
    action:
    - service: homeassistant.turn_on
      data_template:
        entity_id:  >-
          {% if is_state('domain.TOGGLE3', 'off') and ('input_boolean.hasp_plate01_p6_switchtoggles', 'off') -%}
            domain.TOGGLE3
          {%- elif is_state('domain.TOGGLE11', 'off') and ('input_boolean.hasp_plate01_p6_switchtoggles', 'on') -%}
            domain.TOGGLE11
          {%- endif %}

   # Toggle Button 4 when p6.b7 pressed
  - alias: hasp_plate01_p6_Toggle4
    trigger:
    - platform: mqtt
      topic: 'hasp/plate01/state/p[6].b[7]'
      payload: 'ON'
    action:
    - service: homeassistant.turn_on
      data_template:
        entity_id: >-
          {% if is_state('domain.TOGGLE4', 'off') and ('input_boolean.hasp_plate01_p6_switchtoggles', 'off') -%}
            domain.TOGGLE4
          {%- elif is_state('domain.TOGGLE12', 'off') and ('input_boolean.hasp_plate01_p6_switchtoggles', 'on') -%}
            domain.TOGGLE12
          {%- endif %} 
      
   # Toggle Button 5 when p6.b8
  - alias: hasp_plate01_p6_Toggle5_13
    trigger:
    - platform: mqtt
      topic: 'hasp/plate01/state/p[6].b[8]'
      payload: 'ON'
    action:
    - service: homeassistant.turn_on
      data_template:
        entity_id: >-
          {% if is_state('domain.TOGGLE5', 'off') and ('input_boolean.hasp_plate01_p6_switchtoggles', 'off') -%}
            domain.TOGGLE5
          {%- elif is_state('domain.TOGGLE13', 'off') and ('input_boolean.hasp_plate01_p6_switchtoggles', 'on') -%}
            domain.TOGGLE13
          {%- endif %} 
    
    # Toggle Button 6 p4.b9
  - alias: hasp_plate01_p6_Toggle6_14
    trigger:
    - platform: mqtt
      topic: 'hasp/plate01/state/p[6].b[9]'
      payload: 'ON'
    action:
    - service: homeassistant.turn_on
      data_template:
        entity_id: >-
          {% if is_state('domain.TOGGLE6', 'off') and ('input_boolean.hasp_plate01_p6_switchtoggles', 'off') -%}
            domain.TOGGLE6
          {%- elif is_state('domain.TOGGLE14', 'off') and ('input_boolean.hasp_plate01_p6_switchtoggles', 'on') -%}
            domain.TOGGLE14
          {%- endif %} 
    
    # Toggle Button 7 p4.b10
  - alias: hasp_plate01_p6_Toggle7
    trigger:
    - platform: mqtt
      topic: 'hasp/plate01/state/p[6].b[10]'
      payload: 'ON'
    action:
    - service: homeassistant.turn_on
      data_template:
        entity_id: >-
          {% if is_state('domain.TOGGLE7', 'off') and ('input_boolean.hasp_plate01_p6_switchtoggles', 'off') -%}
            domain.TOGGLE7
          {%- elif is_state('domain.TOGGLE15', 'off') and ('input_boolean.hasp_plate01_p6_switchtoggles', 'on') -%}
            domain.TOGGLE15
          {%- endif %} 
        
     # Togge Button 8 p4.b11
  - alias: hasp_plate01_p6_Toggle8
    trigger:
    - platform: mqtt
      topic: 'hasp/plate01/state/p[6].b[11]'
      payload: 'ON'
    action:
    - service: homeassistant.turn_on
      data_template:
        entity_id: >-
          {% if is_state('domain.TOGGLE8', 'off') and ('input_boolean.hasp_plate01_p6_switchtoggles', 'off') -%}
            domain.TOGGLE8
          {%- elif is_state('domain.TOGGLE16', 'off') and ('input_boolean.hasp_plate01_p6_switchtoggles', 'on') -%}
            domain.TOGGLE16
          {%- endif %} 
 
 ##############################################################################################
# Actions for Toggle OFF if pressed

  # Toggle Toggle1/9 when p[6].b[4] pressed
  - alias: hasp_plate01_p6_Toggle1
    trigger:
    - platform: mqtt
      topic: 'hasp/plate01/state/p[6].b[4]'
      payload: 'ON'
    action:
    - service: homeassistant.turn_off
      data_template:
        entity_id: >-
          {% if is_state('domain.TOGGLE1', 'on') and ('input_boolean.hasp_plate01_p6_switchtoggles', 'off') -%}
            domain.TOGGLE1
          {%- elif is_state('domain.TOGGLE9', 'on') and ('input_boolean.hasp_plate01_p6_switchtoggles', 'on') -%}
            domain.TOGGLE9
          {%- endif %}

  # Toggle Toggle2 when p[6].b[5] pressed
  - alias: hasp_plate01_p6_Toggle2
    trigger:
    - platform: mqtt
      topic: 'hasp/plate01/state/p[6].b[5]'
      payload: 'ON'
    action:
    - service: homeassistant.turn_off
      data_template: 
        entity_id: >-
          {% if is_state('domain.TOGGLE2', 'on') and ('input_boolean.hasp_plate01_p6_switchtoggles', 'off') -%}
            domain.TOGGLE2
          {%- elif is_state('domain.TOGGLE10', 'on') and ('input_boolean.hasp_plate01_p6_switchtoggles', 'on') -%}
            domain.TOGGLE10
          {%- endif %}

  # Toggle Toggle3 when p[6].b[6] pressed
  - alias: hasp_plate01_p6_Toggle3
    trigger:
    - platform: mqtt
      topic: 'hasp/plate01/state/p[6].b[6]'
      payload: 'ON'
    action:
    - service: homeassistant.turn_off
      data_template:
        entity_id:  >-
          {% if is_state('domain.TOGGLE3', 'on') and ('input_boolean.hasp_plate01_p6_switchtoggles', 'off') -%}
            domain.TOGGLE3
          {%- elif is_state('domain.TOGGLE4', 'on') and ('input_boolean.hasp_plate01_p6_switchtoggles', 'on') -%}
            domain.TOGGLE4
          {%- endif %}
      
   # Toggle Button 4 when p6.b7 pressed
  - alias: hasp_plate01_p6_Toggle4
    trigger:
    - platform: mqtt
      topic: 'hasp/plate01/state/p[6].b[7]'
      payload: 'ON'
    action:
    - service: homeassistant.turn_off
      data_template:
        entity_id: >-
          {% if is_state('domain.TOGGLE4', 'on') and ('input_boolean.hasp_plate01_p6_switchtoggles', 'off') -%}
            domain.TOGGLE4
          {%- elif is_state('domain.TOGGLE12', 'on') and ('input_boolean.hasp_plate01_p6_switchtoggles', 'on') -%}
            domain.TOGGLE12
          {%- endif %} 
      
   # Toggle Button 5 when p6.b8
  - alias: hasp_plate01_p6_Toggle5_13
    trigger:
    - platform: mqtt
      topic: 'hasp/plate01/state/p[6].b[8]'
      payload: 'ON'
    action:
    - service: homeassistant.turn_off
      data_template:
        entity_id: >-
          {% if is_state('domain.TOGGLE5', 'on') and ('input_boolean.hasp_plate01_p6_switchtoggles', 'off') -%}
            domain.TOGGLE5
          {%- elif is_state('domain.TOGGLE13', 'on') and ('input_boolean.hasp_plate01_p6_switchtoggles', 'on') -%}
            domain.TOGGLE13
          {%- endif %} 
    
    # Toggle Button 6 p4.b9
  - alias: hasp_plate01_p6_Toggle6_14
    trigger:
    - platform: mqtt
      topic: 'hasp/plate01/state/p[6].b[9]'
      payload: 'ON'
    action:
    - service: homeassistant.turn_off
      data_template:
        entity_id: >-
          {% if is_state('domain.TOGGLE6', 'on') and ('input_boolean.hasp_plate01_p6_switchtoggles', 'off') -%}
            domain.TOGGLE6
          {%- elif is_state('domain.TOGGLE14', 'on') and ('input_boolean.hasp_plate01_p6_switchtoggles', 'on') -%}
            domain.TOGGLE14
          {%- endif %} 

 
##############################################################################################
#########################################
# Toggle Color and State Picture

  # Toggle state picture on p[6].b[4] when Toggle [1] changes
  - alias: hasp_plate01_p6_ToggleColor1
    trigger:
    - platform: mqtt
      topic: 'hasp/plate01/state/page'
      payload: '6'
    - platform: state
      entity_id: domain.TOGGLE1
    - platform: state
      entity_id: 'binary_sensor.plate01_connected'
      to: 'on'
    - platform: state
      entity_id: input_boolean.hasp_plate01_p6_switchtoggles
      to: 'off'
    - platform: homeassistant
      event: start
    action:
    - service: mqtt.publish
      data:
        topic: 'hasp/plate01/command/p[6].b[4].picc'
        payload_template: >-
         {% if is_state('domain.TOGGLE1', 'on') and is_state('input_boolean.hasp_plate01_p6_switchtoggles', 'off') -%}
            {{ 1 }}
          {% elif is_state('domain.TOGGLE1', 'off') and is_state('input_boolean.hasp_plate01_p6_switchtoggles', 'off') -%}
            {{ 2 }}
          {%- endif %}

  # Toggle state picture on p[6].b[5] when Toggle [2] changes
  - alias: hasp_plate01_p6_ToggleColor2
    trigger:
    - platform: mqtt
      topic: 'hasp/plate01/state/page'
      payload: '6'
    - platform: state
      entity_id: 'binary_sensor.plate01_connected'
      to: 'on'
    - platform: state
      entity_id: input_boolean.hasp_plate01_p6_switchtoggles
      to: 'off'
    - platform: state
      entity_id: domain.TOGGLE2
    - platform: homeassistant
      event: start
    action:
    - service: mqtt.publish
      data:
        topic: 'hasp/plate01/command/p[6].b[5].picc'
        payload_template: >-
          {% if is_state('domain.TOGGLE2', 'on') and is_state('input_boolean.hasp_plate01_p6_switchtoggles', 'off') -%}
            {{ 1 }}
          {% elif is_state('domain.TOGGLE2', 'off') and is_state('input_boolean.hasp_plate01_p6_switchtoggles', 'off') -%}
            {{ 2 }}
          {%- endif %}

  # Toggle state picture on p[6].b[6] when Toggle [3] changes
  - alias: hasp_plate01_p6_ToggleColor3
    trigger:
    - platform: mqtt
      topic: 'hasp/plate01/state/page'
      payload: '6'
    - platform: state
      entity_id: input_boolean.hasp_plate01_p6_switchtoggles
      to: 'off'
    - platform: state
      entity_id: domain.TOGGLE3
    - platform: state
      entity_id: 'binary_sensor.plate01_connected'
      to: 'on'
    - platform: homeassistant
      event: start
    action:
    - service: mqtt.publish
      data:
        topic: 'hasp/plate01/command/p[6].b[6].picc'
        payload_template: >-
          {% if is_state('domain.TOGGLE3', 'on') and is_state('input_boolean.hasp_plate01_p6_switchtoggles', 'off') -%}
            {{ 1 }}
          {% elif is_state('domain.TOGGLE3', 'off') and is_state('input_boolean.hasp_plate01_p6_switchtoggles', 'off') -%}
            {{ 2 }}
          {%- endif %}
          
  # Toggle state picture on p[6].b[7] when Toggle [4] changes
  - alias: hasp_plate01_p6_ToggleColor4
    trigger:
    - platform: mqtt
      topic: 'hasp/plate01/state/page'
      payload: '6'
    - platform: state
      entity_id: input_boolean.hasp_plate01_p6_switchtoggles
      to: 'off'
    - platform: state
      entity_id: domain.TOGGLE4
    - platform: state
      entity_id: domain.TOGGLE12
    - platform: state
      entity_id: 'binary_sensor.plate01_connected'
      to: 'on'
    - platform: homeassistant
      event: start
    action:
    - service: mqtt.publish
      data:
        topic: 'hasp/plate01/command/p[6].b[7].picc'
        payload_template: >-
          {% if is_state('domain.TOGGLE4', 'on') and is_state('input_boolean.hasp_plate01_p6_switchtoggles', 'off') -%}
            {{ 1 }}
          {% elif is_state('domain.TOGGLE4', 'off') and is_state('input_boolean.hasp_plate01_p6_switchtoggles', 'off') -%}
            {{ 2 }}
          {%- endif %}
   
  # Toggle state picture on p[6].b[8] when Toggle [5] changes
  - alias: hasp_plate01_p6_ToggleColor5
    trigger:
    - platform: mqtt
      topic: 'hasp/plate01/state/page'
      payload: '6'
    - platform: state
      entity_id: input_boolean.hasp_plate01_p6_switchtoggles
      to: 'off'
    - platform: state
      entity_id: domain.TOGGLE5
    - platform: state
      entity_id: domain.TOGGLE13
    - platform: state
      entity_id: 'binary_sensor.plate01_connected'
      to: 'on'
    - platform: homeassistant
      event: start
    action:
    - service: mqtt.publish
      data:
        topic: 'hasp/plate01/command/p[6].b[8].picc'
        payload_template: >-
          {% if is_state('domain.TOGGLE5', 'on') and is_state('input_boolean.hasp_plate01_p6_switchtoggles', 'off') -%}
            {{ 1 }}
          {% elif domain.TOGGLE13', 'off') and is_state('input_boolean.hasp_plate01_p6_switchtoggles', 'off') -%}
            {{ 2 }}
          domain.TOGGLE13
  # Toggle state picture on p[6].b[9] when Toggle [6] changes
  - alias: hasp_plate01_p6_ToggleColor6
    trigger:
    - platform: mqtt
      topic: 'hasp/plate01/state/page'
      payload: '6'
    - platform: state
      entity_id: input_boolean.hasp_plate01_p6_switchtoggles
      to: 'off'
    - platform: state
      entity_id: domain.TOGGLE6
    - platform: state
      entity_id: domain.TOGGLE14
    - platform: state
      entity_id: 'binary_sensor.plate01_connected'
      to: 'on'
    - platform: homeassistant
      event: start
    action:
    - service: mqtt.publish
      data:
        topic: 'hasp/plate01/command/p[6].b[9].picc'
        payload_template: >-
          {% if is_state('domain.TOGGLE6', 'on') and is_state('input_boolean.hasp_plate01_p6_switchtoggles', 'off') -%}
            {{ 1 }}
          {% elif is_state('domain.TOGGLE6', 'off') and is_state('input_boolean.hasp_plate01_p6_switchtoggles', 'off') -%}
            {{ 2 }}
          {%- endif %}
     
  # Toggle state picture on p[6].b[10] when Toggle [7] changes
  - alias: hasp_plate01_p6_ToggleColor7
    trigger:
    - platform: mqtt
      topic: 'hasp/plate01/state/page'
      payload: '6'
    - platform: state
      entity_id: domain.TOGGLE7
    - platform: state
      entity_id: input_boolean.hasp_plate01_p6_switchtoggles
      to: 'off'
    - platform: state
      entity_id: 'binary_sensor.plate01_connected'
      to: 'on'
    - platform: homeassistant
      event: start
    action:
    - service: mqtt.publish
      data:
        topic: 'hasp/plate01/command/p[6].b[10].picc'
        payload_template: >-
          {% if is_state('domain.TOGGLE7', 'on') and is_state('input_boolean.hasp_plate01_p6_switchtoggles', 'off') -%}
            {{ 1 }}
          {% elif is_state('domain.TOGGLE7', 'off') and is_state('input_boolean.hasp_plate01_p6_switchtoggles', 'off') -%}
            {{ 2 }}
          {%- endif %}
         
  # Toggle state picture on p[6].b[11] when Toggle [8] changes
  - alias: hasp_plate01_p6_ToggleColor8
    trigger:
    - platform: mqtt
      topic: 'hasp/plate01/state/page'
      payload: '6'
    - platform: state
      entity_id: domain.TOGGLE8
    - platform: state
      entity_id: input_boolean.hasp_plate01_p6_switchtoggles
      to: 'off'
    - platform: state
      entity_id: 'binary_sensor.plate01_connected'
      to: 'on'
    - platform: homeassistant
      event: start
    action:
    - service: mqtt.publish
      data:
        topic: 'hasp/plate01/command/p[6].b[11].picc'
        payload_template: >-
          {% if is_state('domain.TOGGLE8', 'on') and is_state('input_boolean.hasp_plate01_p6_switchtoggles', 'off') -%}
            {{ 1 }}
          {% elif is_state('domain.TOGGLE8', 'off') and is_state('input_boolean.hasp_plate01_p6_switchtoggles', 'off') -%}
            {{ 2 }}
          {%- endif %}

######################################

# Toggle state picture on p[6].b[4] when Toggle [9] changes
  - alias: hasp_plate01_p6_ToggleColor9
    trigger:
    - platform: mqtt
      topic: 'hasp/plate01/state/page'
      payload: '6'
    - platform: state
      entity_id: input_boolean.hasp_plate01_p6_switchtoggles
      to: 'on'
    - platform: state
      entity_id: domain.TOGGLE9
    - platform: state
      entity_id: 'binary_sensor.plate01_connected'
      to: 'on'
    - platform: homeassistant
      event: start
    action:
    - service: mqtt.publish
      data:
        topic: 'hasp/plate01/command/p[6].b[4].picc'
        payload_template: >-
          {% if is_state('domain.TOGGLE9', 'on') and is_state('input_boolean.hasp_plate01_p6_switchtoggles', 'on') -%}
            {{ 1 }}
          {% elif is_state('domain.TOGGLE9', 'off') and is_state('input_boolean.hasp_plate01_p6_switchtoggles', 'on') -%}
            {{ 2 }}
          {%- endif %}

# Toggle state picture on p[6].b[5] when Toggle [10] changes
  - alias: hasp_plate01_p6_ToggleColor10
    trigger:
    - platform: mqtt
      topic: 'hasp/plate01/state/page'
      payload: '6'
    - platform: state
      entity_id: input_boolean.hasp_plate01_p6_switchtoggles
      to: 'on'
    - platform: state
      entity_id: domain.TOGGLE10
    - platform: state
      entity_id: 'binary_sensor.plate01_connected'
      to: 'on'
    - platform: homeassistant
      event: start
    action:
    - service: mqtt.publish
      data:
        topic: 'hasp/plate01/command/p[6].b[5].picc'
        payload_template: >-
          {% if is_state('domain.TOGGLE10', 'on') and is_state('input_boolean.hasp_plate01_p6_switchtoggles', 'on') -%}
            {{ 1 }}
          {% elif is_state('domain.TOGGLE10', 'off') and is_state('input_boolean.hasp_plate01_p6_switchtoggles', 'on') -%}
            {{ 2 }}
          {%- endif %}

# Toggle state picture on p[6].b[6] when Toggle [11] changes
  - alias: hasp_plate01_p6_ToggleColor11
    trigger:
    - platform: mqtt
      topic: 'hasp/plate01/state/page'
      payload: '6'
    - platform: state
      entity_id: domain.TOGGLE7
    - platform: state
      entity_id: domain.TOGGLE15
    - platform: state
      entity_id: 'binary_sensor.plate01_connected'
      to: 'on'
    - platform: homeassistant
      event: start
    action:
    - service: mqtt.publish
      data:
        topic: 'hasp/plate01/command/p[6].b[6].picc'
        payload_template: >-
          {% if is_state('domain.TOGGLE4', 'on') and is_state('input_boolean.hasp_plate01_p6_switchtoggles', 'on') -%}
            {{ 1 }}
          {% elif is_state('domain.TOGGLE4', 'off') and is_state('input_boolean.hasp_plate01_p6_switchtoggles', 'on') -%}
            {{ 2 }}
          {%- endif %}
          
# Toggle state picture on p[6].b[7] when Toggle [12] changes
  - alias: hasp_plate01_p6_ToggleColor12
    trigger:
    - platform: mqtt
      topic: 'hasp/plate01/state/page'
      payload: '6'
    - platform: state
      entity_id: input_boolean.hasp_plate01_p6_switchtoggles
      to: 'on'
    - platform: state
      entity_id: domain.TOGGLE12
    - platform: state
      entity_id: 'binary_sensor.plate01_connected'
      to: 'on'
    - platform: homeassistant
      event: start
    action:
    - service: mqtt.publish
      data:
        topic: 'hasp/plate01/command/p[6].b[7].picc'
        payload_template: >-
          {% if is_state('domain.TOGGLE12', 'on') and is_state('input_boolean.hasp_plate01_p6_switchtoggles', 'on') -%}
            {{ 1 }}
          {% elif is_state('domain.TOGGLE12', 'off') and is_state('input_boolean.hasp_plate01_p6_switchtoggles', 'on') -%}}
            {{ 2 }}
          {%- endif %}

# Toggle state picture on p[6].b[8] when Toggle [13] changes
  - alias: hasp_plate01_p6_ToggleColor13
    trigger:
    - platform: mqtt
      topic: 'hasp/plate01/state/page'
      payload: '6'
    - platform: state
      entity_id: input_boolean.hasp_plate01_p6_switchtoggles
      to: 'on'
    - platform: state
      entity_id: domain.TOGGLE13
    - platform: state
      entity_id: 'binary_sensor.plate01_connected'
      to: 'on'
    - platform: homeassistant
      event: start
    action:
    - service: mqtt.publish
      data:
        topic: 'hasp/plate01/command/p[6].b[8].picc'
        payload_template: >-
          {% if is_state('domain.TOGGLE13', 'on') and is_state('input_boolean.hasp_plate01_p6_switchtoggles', 'on') -%}
            {{ 1 }}
          {% elif is_state('domain.TOGGLE13', 'off') and is_state('input_boolean.hasp_plate01_p6_switchtoggles', 'on') -%}
            {{ 2 }}
          {%- endif %}

# Toggle state picture on p[6].b[9] when Toggle [14] changes
  - alias: hasp_plate01_p6_ToggleColor14
    trigger:
    - platform: mqtt
      topic: 'hasp/plate01/state/page'
      payload: '6'
    - platform: state
      entity_id: domain.TOGGLE8
    - platform: state
      entity_id: domain.TOGGLE16
    - platform: state
      entity_id: 'binary_sensor.plate01_connected'
      to: 'on'
    - platform: homeassistant
      event: start
    action:
    - service: mqtt.publish
      data:
        topic: 'hasp/plate01/command/p[6].b[9].picc'
        payload_template: >-
          {% if is_state('domain.TOGGLE14', 'on') and is_state('input_boolean.hasp_plate01_p6_switchtoggles', 'on') -%}
            {{ 1 }}
           {% elif is_state('domain.TOGGLE14', 'off') and is_state('input_boolean.hasp_plate01_p6_switchtoggles', 'on') -%}
            {{ 2 }}
          {%- endif %}

# Toggle state picture on p[6].b[10] when Toggle [15] changes
  - alias: hasp_plate01_p6_ToggleColor15
    trigger:
    - platform: mqtt
      topic: 'hasp/plate01/state/page'
      payload: '6'
    - platform: state
      entity_id: input_boolean.hasp_plate01_p6_switchtoggles
      to: 'on'
    - platform: state
      entity_id: domain.TOGGLE15
    - platform: state
      entity_id: 'binary_sensor.plate01_connected'
      to: 'on'
    - platform: homeassistant
      event: start
    action:
    - service: mqtt.publish
      data:
        topic: 'hasp/plate01/command/p[6].b[10].picc'
        payload_template: >-
          {% if is_state('domain.TOGGLE15', 'on') and is_state('input_boolean.hasp_plate01_p6_switchtoggles', 'on') -%}
            {{ 1 }}
          {% elif is_state('domain.TOGGLE15', 'off') and is_state('input_boolean.hasp_plate01_p6_switchtoggles', 'on') -%}
            {{ 2 }}
          {%- endif %}

# Toggle state picture on p[6].b[11] when Toggle [16] changes
  - alias: hasp_plate01_p6_ToggleColor16
    trigger:
    - platform: mqtt
      topic: 'hasp/plate01/state/page'
      payload: '6'
    - platform: state
      entity_id: input_boolean.hasp_plate01_p6_switchtoggles
      to: 'on'
    - platform: state
      entity_id: domain.TOGGLE16
    - platform: state
      entity_id: 'binary_sensor.plate01_connected'
      to: 'on'
    - platform: homeassistant
      event: start
    action:
    - service: mqtt.publish
      data:
        topic: 'hasp/plate01/command/p[6].b[11].picc'
        payload_template: >-
          {% if is_state('domain.TOGGLE16', 'on') and is_state('input_boolean.hasp_plate01_p6_switchtoggles', 'on') -%}
            {{ 1 }}
          {% elif is_state('domain.TOGGLE16', 'off') and is_state('input_boolean.hasp_plate01_p6_switchtoggles', 'on') -%}
            {{ 2 }}
          {% endif %}
          
